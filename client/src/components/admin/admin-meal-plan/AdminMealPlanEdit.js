import React from 'react'
import { connect } from 'react-redux'

const AdminMealPlanEdit = () => {

  return (
    <div>
      Admin Meal Plan Edit
    </div>
  )
}

const mapStateToProps = state => ({

})

export default connect(mapStateToProps, {})(AdminMealPlanEdit)